---
# Surgical playbook: deploy the backup script and desktop shortcut
# without re-running the rest of the bootstrap.
#
# Usage:
#   ansible-playbook playbooks/shrike-backups.yml --ask-pass
- name: Configure backups on shrike
  hosts: windows
  gather_facts: false
  roles:
    - backups
